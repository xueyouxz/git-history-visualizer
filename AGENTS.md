## Agent skills

### Issue tracker

问题、规格和票据使用 GitHub Issues。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认五状态标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

采用 single-context 领域文档结构。详见 `docs/agents/domain.md`。
